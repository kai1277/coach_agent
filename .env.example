# mock = MSWを使ってフロントだけで動かす（開発デフォルト）
# real = 実APIに対してリクエスト（MSWは起動しない）
VITE_API_MODE=mock

# 実APIに切り替えるときのみ設定
# 例) https://your-backend.example.com
VITE_API_BASE_URL=

LLM_ENABLED=false
# 将来用
LLM_BASE_URL=
LLM_API_KEY=